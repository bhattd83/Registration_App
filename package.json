{
  "dependencies": {
    "json-server": "^1.0.0-beta.3",
    "live-server": "^1.2.2"
  },
  "scripts": {
    "live-server": "live-server --ignore=./src/db.json .",
    "json-server": "npx json-server ./src/db.json",
    "start": "npm run json-server & npm run live-server"
  }
}
